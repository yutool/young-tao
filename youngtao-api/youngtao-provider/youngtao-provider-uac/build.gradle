description = "用户认证中心"

// 使用spring boot
apply plugin: 'org.springframework.boot'

dependencies {
    compile project(":youngtao-provider-api:youngtao-provider-uac-api")
}

test {
    useJUnitPlatform()
}
